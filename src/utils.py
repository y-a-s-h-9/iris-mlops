def format_input(sepal_length, sepal_width, petal_length, petal_width):
    return {
        "sepal_length": sepal_length,
        "sepal_width": sepal_width,
        "petal_length": petal_length,
        "petal_width": petal_width
    }
